# cloud: requires the backend origin and uses HttpOnly cookie sessions
VITE_DATA_MODE=cloud
VITE_API_BASE_URL=https://api.example.com

# preview: explicitly enables in-memory, non-persistent preview data
# VITE_DATA_MODE=preview
